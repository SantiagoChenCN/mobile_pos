package com.espsa.mobilepos.app.sync;

public enum ComputerSyncFailureReason {
    CLEAR_TEXT_BLOCKED,
    CONNECTION_TIMEOUT,
    CONNECTION_REFUSED,
    UNKNOWN_HOST,
    INVALID_TOKEN,
    HTTP_ERROR,
    INVALID_RESPONSE,
    INVALID_CONFIG,
    UNKNOWN
}
